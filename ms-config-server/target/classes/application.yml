


#spring.application.name=ms-config-server
server:
  port: 7071




spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/noetipo/DAD-2025-G2.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main
  security:
    user:
      name: root
      password: 123456